repos:
  - repo: local
    hooks:
      - id: update-requirements
        name: Aggiorna e aggiunge requirements.txt
        entry: bash -c "pip list --format=freeze > requirements.txt && git add requirements.txt"
        language: system
        stages: [pre-commit]
